# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Databases
*.db
*.db-shm
*.db-wal
*.sqlite3
*.sqlite3-journal

# Flutter build & deploy duplicates
apex_finance/build/
apex_finance/.dart_tool/
apex_finance/web_deploy/
apex_finance/.packages

# Flutter generated platform files (regenerate on build)
apex_finance/linux/flutter/generated_*
apex_finance/macos/Flutter/GeneratedPluginRegistrant.swift
apex_finance/windows/flutter/generated_*

# Temp / backup files (but NOT __init__.py — that's a real Python file)
_*.py
!__init__.py
!**/__init__.py
*.tmp
*.bak
*.bak_*
*.old
*.orig
*.patch
~*
*.swp
*.swo
*~
.#*

# Presentations (keep out of repo)
*.pptx

# Python test/coverage artifacts
.coverage
.pytest_cache/
htmlcov/
coverage_html/

# Flutter build outputs (deployed via GH Pages from docs/ — still tracked there, not in build/)
apex_finance/build/
# Large HTML prototypes should not be committed
coa_screen.html
**/coa_screen.html

# Docx audit reports (keep in Downloads, not in repo)
*_Audit_Report.docx
/audit_*/
/audit_*.txt

# Data dumps (use templates/ for official templates)
coa_dump.txt
coa_b2.txt

# Deploy temp
gh-pages-temp/

# Environment & secrets
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml

# Logs
*.log

# Phase 1 local backups
_backups/
phase1.ps1
.claude/
apex_audit.txt
workflow_rules.json
approvals.json
webhook_subs.json
comments.json
tenant_modules.json
api_keys.json
custom_roles.json
suggestions.json
industry_pack_assignments.json
tenant_directory.json
workflow_runs.json
activity_feed.json
